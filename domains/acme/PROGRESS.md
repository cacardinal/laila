# Acme Engagement — Session Handoff

> The multi-session handoff file. A session doing engagement work reads this FIRST
> and appends a dated entry at wrap. `tracking/status.md` is the state; this file is
> the narrative — what was tried, what's half-done, what the next session should
> not have to rediscover.

## 2026-07-30 — SOW drafting session

**Done:** SOW draft v1 complete from the proposal v2 terms. Payment schedule matched
to Acme's net-30 (Jordan flagged their AP process is slow — invoice at kickoff, not
at signature).

**Half-done:** The IP clause is still the template's language. Acme's dispatching
model will be trained partly on their historical routing data; the draft doesn't yet
say who owns fine-tuned weights. Flagged for Alex — needs their call, possibly a
lawyer read, before Friday.

**Next session:** resolve the IP clause, final read-through, then present the send
to Alex for approval (client-visible → Tier 3). Do NOT send from a session.

**Gotcha for future sessions:** Jordan's assistant filters attachments from unknown
senders — SOW goes as a link, not a PDF attachment, per Jordan's 7/24 ask.
